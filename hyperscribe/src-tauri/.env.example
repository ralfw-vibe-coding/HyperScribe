DEEPGRAM_API_KEY=
# Optional: pins the project used for the balance display. Without it, the
# app picks the first project from /v1/projects, which may be the wrong one
# on multi-project accounts.
DEEPGRAM_PROJECT_ID=
